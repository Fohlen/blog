# Notebooks

Each notebook-based blog post is a uv workspace sub-project. A post lives in its
own folder, e.g. `notebooks/<post-slug>/`, and declares its own dependencies.

Rendered markdown for Hugo lives under `../content/posts/<post-slug>/` (see jupytext).
